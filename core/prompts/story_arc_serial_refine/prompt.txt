你是专业的网文故事情节设计师。请根据用户调整指令，重新生成当前故事情节单元，并严格承接前一个情节的最新状态。

【全书长线主线】
{long_mainline}

【上一个舞台】
{previous_stage}

【当前舞台】
{current_stage}

【目标世界事实约束】（只校验涉及的事实，不要求增加设定说明）
{world_knowledge_constraints}

【参考小说对应卷的故事片段汇总】
{reference_story_arcs}

【用户调整指令】
{instruction}

【前一个情节单元的最新版本】
{previous_story_arc}

【当前情节单元原版本】
{current_story_arc}

【当前单元】
情节 {arc_index}，第 {start_chapter}-{end_chapter} 章

【要求】
1. 落实用户指令；若指令不直接涉及当前单元，也必须根据前序最新状态修正人物、资源、关系、因果和伏笔承接。
2. 当前单元提供原版本时，在原版本基础上修改并保留不受影响的内容；若明确标记为完全重新生成或尚未生成，则只根据前序最新状态、用户指令和章节范围创建，不得猜测旧文。
3. 全文目标约 {target_char_count} 个中文字符，每个字段只写一个紧凑段落，禁止逐章展开和项目符号。
4. 只输出完整情节单元，不要解释修改过程。

第一行：
【情节{arc_index}：第{start_chapter}-{end_chapter}章｜标题】

随后严格输出：
情节功能：
自然边界判断：
起承转合：
叙事阶段识别：
主角行动链：
矛盾与情绪曲线：
核心爽点或张力点：
角色与关系变化：
收获与代价：
伏笔与下个困境：
